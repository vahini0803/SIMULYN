# The worker takes jobs from Redis; this port only serves /healthz and /readyz.
EXECUTOR_PORT=3002
REDIS_URL="redis://localhost:6379"
EXEC_JOB_CONCURRENCY=4
EXEC_MAX_CONCURRENCY=20
EXEC_TIMEOUT_MS=8000
